events {}

http {
    server {
        listen 80;
        server_name _; 

        location /files/ {
            alias /var/www/html/;  
            autoindex on;
            autoindex_exact_size off;
            autoindex_localtime on;
        }

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}